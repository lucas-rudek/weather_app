import React from "react";
import ReactDOM from "react-dom";

import App from "./App";
document.title = "One Week Weather App";

const rootElement = document.getElementById("root");
ReactDOM.render(<App />, rootElement);
